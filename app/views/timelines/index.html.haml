.container
    .timeline-camera
        = link_to image_tag( 'photo-camera.png' ), new_timeline_path
    - @timelines.each do |timelines|
        .timeline_card
            .timeline_user
                = image_tag @user.avatar_img, class: 'index_avatar'
                .index_username
                    = @user.username
                    = link_to 'Delete', timeline_path(timelines),data: { method: 'delete', confirm: '削除しますか？' }, class: 'timeline_delete'
            .index_create_at
                #{timelines.created_at.year}, #{timelines.created_at.month}, #{timelines.created_at.day}
            .img_container 
                - timelines.album.each_with_index do |album, index|
                    = image_tag album, id: "img_container_album#{index}"
            .timeline_funcsion
                = image_tag 'hart.png', class: 'hart_img'
                = image_tag 'comment.png', class: 'comment_img'
                = link_to image_tag( 'share.png' ), 'https://twitter.com/share?=#{/}', class: 'share_img'
            .timelines_title
                = timelines.title